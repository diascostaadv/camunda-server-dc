# Worker API Gateway - Production Environment
ENVIRONMENT=production

# Network Configuration
NETWORK_DRIVER=overlay

# External Services Mode Configuration
EXTERNAL_SERVICES_MODE=true

# Gateway Configuration
DEBUG=false
GATEWAY_PORT=8080
GATEWAY_REPLICAS=2

# MongoDB Configuration (for local container - only used when EXTERNAL_SERVICES_MODE=false)
MONGO_USERNAME=gateway_admin
MONGO_PASSWORD=gateway_prod_password_2024
MONGODB_PORT=27017
MONGODB_DATABASE=worker_gateway

# RabbitMQ Configuration (for local container - only used when EXTERNAL_SERVICES_MODE=false)
RABBITMQ_USER=gateway_admin
RABBITMQ_PASSWORD=gateway_prod_password_2024
RABBITMQ_PORT=5672
RABBITMQ_MGMT_PORT=15672
RABBITMQ_VHOST=worker_gateway
RABBITMQ_EXCHANGE=worker_gateway

# Redis Configuration (for local container - only used when EXTERNAL_SERVICES_MODE=false)
REDIS_PORT=6379

# External Services URIs - Azure Managed Services
# Replace with your actual Azure service connection strings
MONGODB_URI=mongodb+srv://camunda:Rqt0wVmEZhcME7HC@camundadc.os1avun.mongodb.net/?retryWrites=true&w=majority&appName=CamundaDC
RABBITMQ_URL=amqp://localhost:5672/
REDIS_URI=redis://localhost:6379

# Task Processing Configuration
TASK_TIMEOUT=600
TASK_RETRY_LIMIT=5
TASK_RETRY_DELAY=120

# Logging Configuration
LOG_LEVEL=INFO

# Metrics Configuration
METRICS_ENABLED=true
METRICS_PORT=9000