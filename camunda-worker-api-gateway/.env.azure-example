# Worker API Gateway - Azure Environment Example
ENVIRONMENT=production

# Network Configuration
NETWORK_DRIVER=overlay

# External Services Mode Configuration
EXTERNAL_SERVICES_MODE=true

# Gateway Configuration
DEBUG=false
GATEWAY_PORT=8000
GATEWAY_REPLICAS=2

# MongoDB Configuration (not used in external mode)
MONGO_USERNAME=unused_in_external_mode
MONGO_PASSWORD=unused_in_external_mode
MONGODB_PORT=27017
MONGODB_DATABASE=worker_gateway

# RabbitMQ Configuration (not used in external mode)
RABBITMQ_USER=unused_in_external_mode
RABBITMQ_PASSWORD=unused_in_external_mode
RABBITMQ_PORT=5672
RABBITMQ_MGMT_PORT=15672
RABBITMQ_VHOST=worker_gateway
RABBITMQ_EXCHANGE=worker_gateway

# Redis Configuration (not used in external mode)
REDIS_PORT=6379

# External Services URIs - Azure Managed Services
# Azure Cosmos DB for MongoDB
MONGODB_URI=mongodb+srv://camunda:Rqt0wVmEZhcME7HC@camundadc.os1avun.mongodb.net/?retryWrites=true&w=majority&appName=CamundaDC

# Azure Service Bus (alternative to RabbitMQ)
# RABBITMQ_URL would be replaced with Service Bus connection string
RABBITMQ_URL=Endpoint=sb://your-servicebus.servicebus.windows.net/;SharedAccessKeyName=RootManageSharedAccessKey;SharedAccessKey=your-shared-access-key

# Azure Cache for Redis
REDIS_URI=rediss://your-redis-cache.redis.cache.windows.net:6380?password=your-redis-access-key&ssl_cert_reqs=required

# Task Processing Configuration
TASK_TIMEOUT=600
TASK_RETRY_LIMIT=5
TASK_RETRY_DELAY=120

# Marcação Automática de Publicações
# true = Marca automaticamente (recomendado para produção)
# false = Desabilita marcação automática
MARCAR_AUTOMATICAMENTE=true

# Logging Configuration
LOG_LEVEL=INFO

# Metrics Configuration
METRICS_ENABLED=true
METRICS_PORT=9000

# Azure-specific settings
# Connection timeouts for Azure services
MONGODB_CONNECT_TIMEOUT=30000
MONGODB_SERVER_SELECTION_TIMEOUT=30000
REDIS_CONNECT_TIMEOUT=10000
RABBITMQ_CONNECTION_TIMEOUT=30000